// @flow

const colors = {
  POSITIVE: '#F17105',
  NEGATIVE: '#2E4057',
};

const pieConfig = {
  RADIUS: 200,
  INNER_RADIUS: 80
};

const labels = {
  POSITIVE: 'positive',
  NEGATIVE: 'negative',
}

export { colors, pieConfig, labels };
